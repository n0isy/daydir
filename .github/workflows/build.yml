name: Build and release

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y nasm python3

      - name: Assemble the executable
        run: python3 build.py

      - name: Upload executable and documentation
        uses: actions/upload-artifact@v7
        with:
          name: daydir-windows-x64
          if-no-files-found: error
          path: |
            dist/daydir.exe
            README.md
            HEX-ANALISYS.md

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      actions: read
    steps:
      - name: Download the build artifact
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
        run: gh run download "$RUN_ID" --name daydir-windows-x64 --dir artifact

      - name: Publish the versioned release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          gh release create "$RELEASE_TAG" \
            artifact/dist/daydir.exe \
            artifact/README.md \
            artifact/HEX-ANALISYS.md \
            --title "daydir $RELEASE_TAG" \
            --notes "A Windows x64 utility that creates today's directory next to the executable and removes yesterday's directory only when empty. Built by GitHub Actions."
